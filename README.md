# OneMapAPI_BT4015
onemap API script pull for bt4015 group 2. some script are already pre-written for export to csv 
please signup for a new account on the onemap API then put ur email and user in the env folder. u can then use get_token.py to generate ur own token. recommend calling new token each tiem u pull api as it rotates like every day or sth like that
